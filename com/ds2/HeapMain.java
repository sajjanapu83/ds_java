import java.util.Arrays;

public class HeapMain {
    public static void main(String[] args) {
        // FirstHeap heap = new FirstHeap(10);
        // heap.insertNode(15);
        // heap.insertNode(10);
        // heap.insertNode(3);
        // heap.insertNode(8);
        // heap.insertNode(12);
        // heap.insertNode(9);
        // heap.insertNode(4);
        // heap.insertNode(1);
        // heap.insertNode(24);
        // int value =  heap.remove();
        // System.out.println(value);


        // Heap heap = new Heap(10);
        // heap.insert(15);
        // heap.insert(10);
        // heap.insert(3);
        // heap.insert(8);
        // heap.insert(12);
        // heap.insert(9);
        // heap.insert(4);
        // heap.insert(1);
        // heap.insert(24);
        // int value = heap.remove();
        // System.out.println(value);

        // Implementing heap sort

        // int [] numbers  = {5,  7, 4, 8, 3, 2};
        // var heap = new  Heap(numbers.length);

        // for(var number : numbers)
        //     heap.insert(number);
        
        // //descending 
        // // for (int i=0 ; i < numbers.length; i++)
        // //     numbers[i]  = heap.remove();
        // //ascending
        // for (int i=numbers.length -1; i >=0; i--)
        //     numbers[i] = heap.remove();

        // System.out.println(Arrays.toString(numbers));


        // Heapify
        // int [] numbers  = {7, 8, 4, 5, 3, 2};
        // FirstMaxHeap.heapify(numbers);
        // System.out.println(Arrays.toString(numbers));


        //get Kth element
        // int [] numbers  = {7, 8, 4, 5, 3, 2};
        // int value = MaxHeap.getKthLargestItem(numbers, 4);
        // System.out.println(value);
    }
    
}
